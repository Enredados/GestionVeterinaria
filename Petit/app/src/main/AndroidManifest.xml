<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.enredados.petit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Petit">

        <activity android:name=".GUI.EliminarMed"></activity>
        <activity android:name=".GUI.ConsultaGeneralMed" />
        <activity android:name=".GUI.ConsultaParametroMed" />

        <activity android:name=".GUI.HistorialCitasDescripcionActivity"></activity>

        <activity
            android:name=".GUI.dueno"
            android:label="Clientes"
            android:parentActivityName=".GUI.HomeActivity" />
        <activity
            android:name=".GUI.registro_dueno"
            android:label="Agregar Cliente"
            android:parentActivityName=".GUI.dueno" />
        <activity
            android:name=".GUI.RegistroActivity"
            android:parentActivityName=".GUI.AuthActivity" />
        <activity
            android:name=".GUI.HomeActivity"
            android:label="Menú Principal" />
        <activity android:name=".GUI.AuthActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GUI.IngresoMed"
            android:parentActivityName=".GUI.HomeActivity" />
    </application>

</manifest>